var user = require('../models/admin/users');
module.exports = {
    index : (req,res)=>{

        // user.findOne().exec((err,res)=>{

        //     console.log(res)

        // })
        
        
    }
}